import { View, Text } from 'react-native'

export default function Series() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", width: "70%", paddingHorizontal: 30 }}>
      <Text>Series wont be available just yet but we are working on them</Text>
    </View>
  )
}