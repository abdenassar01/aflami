import { ScrollView, Text } from "react-native";
import styled from "styled-components";

export const SuggestionsWrapper = styled(ScrollView)`

`

export const Heading = styled(Text)`
    text-align: center;
    color: red;
    font-size: 13px;
`